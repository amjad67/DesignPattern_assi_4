# DesignPattern_assi_4
i create color class contain main 3 colors as a integer value, constructer, and get color name method , this class extends colorName class which is cotain method to get the name of the color based on colors range, and I created the coloreStore class  contain get color name method and add color method which is verify the color ranges (it have to be btw 0-255) and create a new color and add it to hashmap or returned it if it already exist ,and this class extends VerifyColor class which contain all those method that helps to verify range and creation for color
